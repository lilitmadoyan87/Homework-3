function sumOfEachRow(arr) {
  let arrNew = [];
  for(let i = 0; i < arr.length; i++) {
    let sum = 0;
    for(let j = 0; j < arr[i].length; j++) {
      sum +=arr[i][j]
    } 
    arrNew.push(sum)
  }
  return arrNew;
}

let arr = [[1, 8, 1], [-2, 3, 4], [1, 0]];
console.log(sumOfEachRow(arr));
